{
  "name": "shopify-inventory-sync",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "@netlify/functions": "^1.2.0",
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {
    "@netlify/plugin-env": "^2.0.0"
  }
}
